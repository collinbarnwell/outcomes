%h1 Listing alums

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @alums.each do |alum|
      %tr
        %td= link_to 'Show', alum
        %td= link_to 'Edit', edit_alum_path(alum)
        %td= link_to 'Destroy', alum, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Alum', new_alum_path
